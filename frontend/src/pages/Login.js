import { Box } from "@chakra-ui/react";
import LoginCard from "../components/Login/LoginCard";

const Login = () => {
    return (
        <Box>
            <LoginCard />
        </Box>
    );
};
export default Login;
